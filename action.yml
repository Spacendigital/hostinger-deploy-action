name: 'Hostinger Deploy'
description: 'Deploy a Next.js app to Hostinger via SSH with GitHub Deployments tracking.'
author: 'Spacendigital'

inputs:
  host:
    description: 'Server hostname or IP (store as secret)'
    required: true
  username:
    description: 'SSH username (store as secret)'
    required: true
  password:
    description: 'SSH password'
    required: false
  private-key:
    description: 'SSH private key content (alternative to password)'
    required: false
  port:
    description: 'SSH port (store as secret)'
    required: false
    default: '22'
  target-dir:
    description: 'Project directory on the server. Auto-detected from username if omitted.'
    required: false
  build-command:
    description: 'Build command to run on the server'
    required: false
    default: 'npm run build'
  install-command:
    description: 'Install command'
    required: false
    default: 'npm ci'
  deploy-mode:
    description: 'ssh or sftp'
    required: false
    default: 'ssh'
  clean:
    description: 'Delete all files in target-dir before uploading (sftp only)'
    required: false
    default: 'false'
  environment:
    description: 'GitHub deployment environment name'
    required: false
    default: 'production'
  live-url:
    description: 'Live site URL. If omitted, extracts domain from target-dir path or auto-detects.'
    required: false
  source-dir:
    description: 'Directory to upload (sftp only)'
    required: false
    default: 'out'

runs:
  using: 'node20'
  main: 'dist/index.js'

branding:
  icon: 'upload-cloud'
  color: 'orange'
